# 更新日志

本文件记录 Octo 聊天增强每个公开版本中对使用者有影响的变化。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。提交记录和内部重构不作为发布说明。

## [Unreleased]

## [0.7.0] - 2026-08-26

### 新增

- 外部链接新增快捷操作按钮，可直接执行常用链接操作。
- 新增「像素游乐场」主题，包含像素风聊天气泡、全站配色、资料卡样式与交互效果。

## [0.6.4] - 2026-08-25

### 修复

- 适配 Octo 消息正文新增的点击代理层：避免两层 `fit-content` 嵌套时把中文气泡压缩到单字宽度，短消息不再竖向换行，长消息仍按内容自适应并保留最大宽度。

## [0.6.3] - 2026-08-14

### 新增

- 消息美化新增 LaTeX 公式渲染：支持 `$...$`、`$$...$$`、`\(...\)`、`\[...\]`，也能识别单独粘贴的公式、Markdown 拆开的块公式和公式代码块；使用 KaTeX 转为浏览器原生 MathML，关闭美化时会恢复原始源码。

### 修复

- 修复 Octo Markdown 吃掉 `\left\{` / `\right\}` 中花括号转义后导致复杂公式显示为红色源码的问题；同时避免把普通聊天、代码、Windows 路径、未知 TeX 命令和残缺分隔符误判成公式。

## [0.6.1] - 2026-08-12

### 优化

- 会话折叠入口不再注入 Octo 原生右键菜单，也不再额外占用会话行宽度。现在顶部新增「章鱼折叠入口」开关；开启后用粉色章鱼头像替换原会话头像，点击即可折叠或恢复该会话，平时关闭则完全保留原头像和原列表布局。

## [0.6.0] - 2026-08-12

### 新增

- 插件设置新增独立的「会话列表」Tab，统一收纳重要性排序、会话行精简和折叠，不再让会话类能力散落在通用功能列表中。
- 新增手动会话折叠：插件不接管 Octo 官方置顶规则；「折叠的会话」使用紧凑分组栏，显示数量和未读状态，点开后折叠项以轻缩进、间距、圆角浅色行紧跟入口展开，不再使用重复蓝色竖线，也不会被“一周前自动收起”推到列表末尾。行内操作使用避开红点与未读徽章、默认无底色的轻量「折叠 / 恢复」悬停按钮；折叠状态按 Octo 账号和 Space 保存在插件本地，也支持插件内「全部恢复」。原「只看最近一周」同步迁入折叠设置，作为独立的自动折叠规则。

### 修复

- 修复 Side Panel 偶发显示「部分增强可能已失效」的误报：聊天容器已经挂载、但空会话或消息历史尚未渲染时，旧自检会把同时缺少的两类消息行误判为 Octo 改版；而首次健康结果又因空指纹与初始值相同而不会写回，导致历史警告一直残留。现在消息节点会相互佐证，空会话不报警，页面首次健康检查也会主动清除旧报告。
- 「更早的 N 个会话」展开后显示错位：同时开着「按重要性排序」时，被揭示的老会话会被甩到列表**最上面**（一条读过的老会话落在排序的「其它」档，而它比免打扰未读靠前），于是点开「更早的 62 个会话」反而在顶部冒出四个月前的东西。现在它们按住在列表末尾（还是时间倒序），脚注仍在最后一行；展开/收起后会把脚注带回视野，不再点一下「好像没反应」。
- 「单行」档下隐藏头像上的活跃时间胶囊（如「22分钟」）：它宽 47px、靠右定位，在 26px 的头像旁边会从头像左侧 11px 外一直铺到标题下面。在线与否已经由那个 7px 的绿点表达。
- 「单行」档下，1v1 和 AI 会话的头像变成一整块纯绿圆点。原因是头像盒里还压着一个绝对定位的在线状态小点（9px、成功色绿），缩小头像的规则把它一起放大到 26px，于是它铺满整个头像盖住了图片；现在只缩头像本体，状态点按比例收小。群头像没有这个角标，所以此前只有私聊和 AI「变绿」。

### 变更

- 「单行」档不再在标题前面显示子区所属的群名。侧边栏只有 291px，那个前缀要吃掉近 90px 去渲染一个被截成「FT-OctoCore…」的名字，识别不了任何东西；单行模式下你要的就是子区名本身，它挂在哪个群下不影响你点哪里。现在标题拿回整行宽度，鲜了不少。想看归属的两条路都还在：**鼠标悬停在行上**会提示「父群 · 名称 · 时间」（这也把单行档隐藏的时间补上了），或者开到「连续折叠」档——那里群名以分组表头的形式回来，一个表头能罩住好几行。
- 「只看最近一周」现在也会折叠**免打扰会话里堆积的未读**。之前只要有未读就一律豁免，结果实测 62 个一周外会话里唯一没被折叠的两个，都是三十多天前、只剩一个灰点的免打扰子区 —— 用户已经说过「这里别烦我」，再拿它的未读数抵消一周过滤没有意义。@我 的会话（含免打扰群里的 @我）和未读私聊仍然永不折叠，置顶行也照旧。

## [0.5.0] - 2026-08-06

### 新增

- 「舒适输入框」底部新增 **5 个快捷 @ 头像**：读这个会话的历史记录，统计谁被 @ 得最多（谁发的都算，自己发的算双倍，越近的权重越高），把最常被 @ 的 5 个排在那里，点一下就在输入框里 @ 他 —— 不用再打 `@` 然后滚动找人。插入的是和手打 `@` 选人完全一样的真 @（对方会收到提醒），而不是一段长得像 @ 的文字。AI 会带紫色角标，单聊里不显示。
- 消息列表里自己的头像比别人稍大一圈，更容易一眼分辨谁是自己；鼠标移到自己的头像上会先慢慢转起来、再越转越快，移开即恢复。只是滑过头像不会触发动画，头像原有的点击行为照旧。系统开启「减少动态效果」时只放大不旋转。

### 移除

- **「显示已撤回的消息」整体下线**，相关代码全部删除（约 450 行页面脚本、开关、样式与消息协议）。原因是它的能力上限太低而不好说明：服务器在消息被撤回后就不再返回原文，唯一的来源是那条消息当初正常送达时进入页面内存的那一份 —— 也就是说只有「那个会话当时正开着」才还原得出来，你在别的会话或没开浏览器时一律无效，而后者恰恰是人们想要这个功能的时刻。与其留一个「不保证能用」的功能，不如去掉。
- 随之删除了 `octoRecallEnabled` 这个设置项（升级后不再出现在设置页），以及内部的 `toggle` 消息协议；`utils/octoRecall.ts` 更名为 `utils/octoShared.ts`。

### 变更

- 插件定位改成「换个好看的界面 + 把常用操作变快」：扩展描述与 README 的功能顺序按实际价值重排（美化换肤、舒适输入框与快捷 @、全站主题、GitHub 跳转在前）。
- 扩展 ID 固定为 `pcofpmfiknglflncnjejfldadchndnoo`（Firefox：`octo-chat-enhancer@botshen.github.io`）。之前 ID 由安装目录的路径推导，把 Release 包解压到另一个文件夹再加载就会被浏览器当成另一个扩展，导入的宠物和全部开关跟着丢失；现在换目录、换机器重装都是同一个扩展，设置保留。**从旧版升级到本版时，旧安装的 ID 会变，因此需要重新导入宠物并设置一次（仅此一次）。**

## [0.4.0] - 2026-08-06

### 优化

- 安装包体积从 3.4 MB 降到 1.5 MB，每次打开 Octo 注入的脚本从 1.07 MB 降到 308 KB；球星射门特效改为首次启用时才下载，没开过的用户不再为它付费。
- 长会话下的消息处理明显变快：3000 条消息时单条新消息的处理耗时从 9.1 ms 降到 1.0 ms，且不再随会话变长而持续变慢。
- 删除从未被使用的球星旋转序列图和重复的水印图，剩余素材转为 WebP。
- 内置「蜗牛巡游」放慢到符合蜗牛的速度：走完一趟从 22 秒变成 34 秒，身体摆动也同步变慢。

### 新增

- 「消息美化与主题」现在有自己的开关，可以在保留其它增强的情况下单独关掉美化和换肤。
- 当 Octo 页面结构改版导致部分增强失效时，设置页会直接提示具体是哪项能力不可用，而不再是默默失效。

### 变更

- 扩展图标和设置页品牌图换成新的粉色小章鱼。
- 「全部增强」总开关移到设置页顶部的标题栏，不再和各功能的开关排在一起——它不是「再多一个开关」，关掉等于页面回到没装扩展的样子。暂停时会明确提示；此时设置照常可改，重新启用后生效。
- 设置页重做成统一形态：每项功能一行 —— 名称、当前状态、自己的开关，细节默认折叠，展开一项会收起其它项。原来所有功能的所有设置同时铺在页面上，超过四五项之后就很难看出「什么开着」。

### 修复

- 修复设置页「输入框宠物」的四张选择卡紧贴卡片边框，左右没有内边距，与同一卡片内的其他行没对齐。
- 修复内置「蚂蚁小队」朝向与前进方向相反，看起来像在倒着走：现在蚂蚁会按行进方向镜像（蜗牛、巫师、僵尸的朝向本来就是对的，保持不变）。
- 修复一个安全问题：Octo 页面上的脚本可以伪造指令，让插件向任意外部地址发起图片请求（可用于探测谁装了插件，在内网部署下还是一条外发通道）。现在宠物图只接受本地内嵌图片。

## [0.3.0] - 2026-08-05

### 新增

- 设置页新增「启用全部增强」总开关；关闭后会还原页面并暂停消息恢复、主题美化、射门特效和桌面宠物，再次开启时恢复此前设置。
- 完整设置从 Popup 迁移到可常驻的 Side Panel，点击扩展图标即可打开，调整主题、特效和桌宠时可以同时查看网页效果。
- 扩展图标和设置页品牌图替换为新的 Doge 章鱼 Logo。
- 新增「舒适输入框」，提供更宽裕的多行编辑空间，并将工具栏调整到右下角。
- 内置蚂蚁、蜗牛、巫师和僵尸四种输入框宠物，输入或收到新消息时会在当前输入框上方巡游。
- 消息中的 GitHub 仓库、Issue、PR、Commit、Action、Release 和文件链接新增快捷入口，并修正链接后紧跟文字时的跳转地址。

### 优化

- 优化总开关的页面还原与启动时机，并改进设置保存反馈、主题选择键盘操作和设置页加载速度。
- 修复消息美化的左右留白撑宽聊天区域、导致底部出现横向滚动条的问题。

## [0.2.11] - 2026-08-05

### 新增

- 企业微信会议与日程链接可在聊天中渲染为内联二维码卡片；会议卡片支持通过腾讯会议客户端直接加入。

### 优化

- 含企业微信链接卡片的消息不再受长消息高度折叠影响，确保二维码完整可扫描。

## [0.2.10] - 2026-07-31

### 新增

- 新增 QQ 2012 经典与 QQ 2014 气泡两套消息区皮肤，并支持 QQ 2014「自己的消息靠左」开关。

## [0.2.9] - 2026-07-30

### 新增

- 桌宠新增当前会话新消息短气泡，支持文本摘要和图片、视频、语音、文件等类型提示，并过滤自己发送、系统、撤回及重复消息。

## [0.2.8] - 2026-07-30

### 新增

- 桌面宠物支持 manifest 动作映射与状态机：静止、鼠标悬浮、左右拖动会切换不同动作，松开后恢复悬浮或静止状态。
- 兼容 Codex v1 `8 × 9` 与 v2 `8 × 11` atlas，并识别标准动作行、逐帧时长和 v2 方向帧。

### 优化

- 宠物包导入新增网格、动作帧、帧率/时长、循环配置和状态引用校验，同时保留旧版 `12 × 13` 第一行动画兼容逻辑。
- 兼容早期制包示例中的 `sprite.{columns,rows,defaultFps}` 与 `states.{default,hover,dragging,dragLeft,dragRight}` 别名，冲突配置会在导入时拒绝。

## [0.2.7] - 2026-07-30

### 新增

- 新增「本地桌面宠物」：可在扩展弹窗导入 `.zip` / `.codex-pet.zip` 宠物包，在 Octo 页面右下角显示可拖拽、记忆位置的 spritesheet 帧动画宠物。
- 宠物包解析全程在浏览器本地完成，校验 `pet.json`、spritesheet 路径、文件数量、大小和图片可解码状态；支持 WebP / PNG / JPG / GIF。

### 优化

- 扩展说明与权限描述补充本地桌面宠物能力，并增加 `unlimitedStorage` 以容纳本地宠物素材。

## [0.2.3] - 2026-07-10

### 新增

- Bot 资料卡改版为「全息卡牌」：synthwave 落日 banner + 悬浮圆形大头像，备注/简介等信息合并进一个连续大框，创建者移到卡片底部作署名；卡片随鼠标 3D 倾斜并有跟手的全息高光。亮色 / 暗色 / 世界杯三套主题各自配色，结构共享。纯 CSS 覆盖 + 只读 DOM，不改动 Octo 源码；`prefers-reduced-motion` 下自动关闭 3D 倾斜与全息高光。

## [0.2.2] - 2026-07-10

### 新增

- Bot 资料卡「开卡抽卡」：每次打开 Bot 资料卡随机抽取一个稀有度（宝可梦式档位 N / R / SR / SSR / UR，越稀越少），并据此渲染金箔全息卡框、左上角稀有度角标与高档档位的外发光脉动；抽到 SR 及以上会播放一段全屏揭晓特效（SSR/UR 追加放射光线，UR 追加彩虹光爆与亮片）。纯前端随机 + CSS 覆盖，不改动 Octo 源码；`prefers-reduced-motion` 下自动关闭动效。

## [0.2.1] - 2026-07-10

### 新增

- 新增「鼠标变足球」独立开关：可单独开关足球光标，关闭后保留系统光标、点击仍可射门。

### 优化

- 优化射门特效渲染性能（关闭无用的交互命中测试、降低全局辉光开销、球体缩放直算），点击射门时更省资源。
- 设置弹窗改为多列布局，容纳更多配置项。

## [0.2.0] - 2026-07-10

- 「消息美化与主题」现在有自己的开关，可以在保留其它增强的情况下单独关掉美化和换肤。

### 变更

- 扩展图标和设置页品牌图换成新的粉色小章鱼。
- 「全部增强」总开关移到设置页顶部的标题栏，不再和各功能的开关排在一起——它不是「再多一个开关」，关掉等于页面回到没装扩展的样子。暂停时会明确提示；此时设置照常可改，重新启用后生效。
- 设置页重做成统一形态：每项功能一行 —— 名称、当前状态、自己的开关，细节默认折叠，展开一项会收起其它项。原来所有功能的所有设置同时铺在页面上，超过四五项之后就很难看出「什么开着」。

- 世界杯足球射门动画改用 PixiJS/WebGL 渲染（替代原 Canvas 2D 实现），点击射门与拖动/滚动页面时更流畅。

### 新增

- 射门改为弧圈球轨迹，每次点击的弯曲方向和弧度都不一样。
- 为射门加入缎带拖尾、全局辉光（Bloom）与命中冲击波特效。

### 优化

- 移除滚动时的画布重算、精简全站光标样式选择器，并收窄「显示已撤回消息」的 DOM 监听范围，降低拖动与点击时的卡顿。

## [0.1.1] - 2026-07-10

### 新增

- 新增姆巴佩主题水印，可与梅西水印互斥单选并显示在聊天区域右下角。

## [0.1.0] - 2026-07-10

### 新增

- 还原 Octo 中已撤回消息的原文，并以「已撤回」标记区分。
- 提供赛博紫亮色、赛博紫暗色和美加墨世界杯三套消息主题。
- 提供跟随原站、赛博紫亮色、赛博紫暗色、雾青工作台和美加墨世界杯五套全站配色。
- 为世界杯主题提供闪电爆射、火焰弹道、子弹时间、彗星光轨和重炮轰门五种足球动画。
- 支持在聊天区域显示梅西主题水印。
- 主题、特效和功能开关保存在浏览器本地，刷新页面后继续生效。
- 提供 Chrome Release ZIP、自动发布流程和 SHA-256 校验文件。

### 修复

- 还原撤回消息时正确显示原发送者姓名和头像。

[Unreleased]: https://github.com/botshen/octo-help/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/botshen/octo-help/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/botshen/octo-help/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/botshen/octo-help/compare/v0.2.8...v0.3.0
[0.2.8]: https://github.com/botshen/octo-help/compare/v0.2.7...v0.2.8
[0.2.7]: https://github.com/botshen/octo-help/compare/v0.2.5...v0.2.7
[0.2.3]: https://github.com/botshen/octo-help/compare/v0.2.2...v0.2.3
[0.2.2]: https://github.com/botshen/octo-help/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/botshen/octo-help/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/botshen/octo-help/compare/v0.1.1...v0.2.0
[0.1.1]: https://github.com/botshen/octo-help/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/botshen/octo-help/releases/tag/v0.1.0
